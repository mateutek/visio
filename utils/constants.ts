export const GATHERING_PROFESSIONS = [182, 186, 356, 393];

export type Settings = {
  raidDefaultListView: boolean;
  charactersDefaultListView: boolean;
  charactersImageBg: boolean;
};

export const defaultSettings: Settings = {
  raidDefaultListView: false,
  charactersDefaultListView: false,
  charactersImageBg: false,
};
